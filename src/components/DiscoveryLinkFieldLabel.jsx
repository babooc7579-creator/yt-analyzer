export default function DiscoveryLinkFieldLabel({ children }) {
  return (
    <label className="text-[11px] font-extrabold uppercase tracking-wide text-slate-500">
      {children}
    </label>
  );
}
