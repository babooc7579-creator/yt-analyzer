# Creator OS 개선 기록 운영 기준

## 목적

Creator OS를 고도화할 때 현재 상태, 목표 방향, 다음 작업, 결정 필요 사항을 잃지 않도록 관리합니다.
기능이 많아지는 것보다 실제 사용자 흐름과 이름·동작·데이터 결과가 일치하는지를 우선합니다.

## 화면과 기록 원본

- 사용자 화면: `인사이트 / 학습 → 개선 기록`
- 구조화된 기록 원본: `src/constants/improvementLog.js`
- 화면 컴포넌트: `src/components/ImprovementLogWorkspace.jsx`
- 정기 검수 기준: `docs/CREATOR_OS_REGRESSION_CHECKLIST.md`
- 변경 추적: Git 커밋과 Pull Request

개선 기록 화면은 읽기 전용입니다. 화면을 열어도 API 호출, 온라인 저장소(Azure DB) 조회·저장,
YouTube API 수집, localStorage 변경이 발생하지 않습니다.

## 체크포인트 상태

| 상태 | 의미 |
|---|---|
| 확인 완료 | 실제 구현과 사용자에게 보이는 결과를 확인했습니다. |
| 진행 중 | 일부가 구현됐지만 검수나 후속 작업이 남았습니다. |
| 결정 필요 | 데이터 구조, 메뉴 통합, 비용 또는 사용자 선택이 필요합니다. |
| 진행 예정 | 방향은 정해졌으며 앞선 작업 뒤에 진행합니다. |
| 나중에 | 개인용 MVP 이후로 미룹니다. |

## 완료 판단 기준

아래를 필요한 범위까지 확인한 뒤에만 `확인 완료`로 바꿉니다.

1. 코드 또는 버튼이 존재합니다.
2. 실제로 클릭할 수 있습니다.
3. 사용자가 화면에서 변화를 확인할 수 있습니다.
4. 결과 위치와 다음 행동이 명확합니다.
5. 버튼 이름과 실제 기능이 일치합니다.
6. 필요한 경우 API 호출과 온라인 저장소(Azure DB) 결과를 확인했습니다.
7. YouTube 신규 수집과 Azure DB 조회·저장을 구분했습니다.

## 기능 변경 시 갱신 순서

1. 변경 대상 영역의 현재 체크포인트를 읽습니다.
2. 이번 변경 범위와 완료 기준을 정합니다.
3. 코드와 테스트를 작은 단위로 수정합니다.
4. 운영 화면에서 실제 결과를 확인합니다.
5. `src/constants/improvementLog.js`의 상태, 다음 작업, 마지막 점검일을 갱신합니다.
6. 중요한 결정은 `decisions`에 남깁니다.
7. `docs/HANDOFF.md`와 `docs/WORK_LOG.md`에 실제 완료 내용을 기록합니다.

## 메뉴 역할과 확인 상태

개선 기록 화면의 `메뉴 역할표`에서 다음을 함께 관리합니다.

- 메뉴가 존재하는 이유
- 실제 데이터·동작 경계
- 운영 클릭, 로컬 클릭, 코드·테스트, 결정 필요 중 현재 확인 수준

운영 화면을 누르지 않은 기능은 코드나 테스트가 있어도 `운영 클릭 확인`으로 표시하지 않습니다.

## 대본 작업실 현재 기준

- 제작 후보함: 무엇을 만들지 결정합니다.
- 대본 작업실: 결정한 소재를 어떻게 만들지 분석·작성·수정합니다.
- 업로드 캘린더: 언제 올릴지 확인합니다.
- AI 공방: 이후 분석과 작성 제안을 보조합니다.

현재 백엔드는 기존 `draftTitle`, `note`, `targetPublishDate`와 함께
`scriptAnalysis`, `scriptOutline`, `scriptBody`, `scriptStatus`를 보존합니다.
기존 `note`는 변환하거나 삭제하지 않고 `기존 통합 작업 메모`로 유지합니다.
수정 이력, 이전 버전 비교, AI 보조는 개인용 MVP 이후의 별도 작업입니다.

## 2026-07-28 완료한 1~5번 연결 검수

- 발견함에서 제작 후보로 표시한 링크는 제작 후보함에 이미 연결돼 있습니다.
- 제작 후보함의 링크 카드에 `대본 작업 시작`을 추가해 같은 링크를 대본 작업실 원본으로 사용합니다.
- 링크 대본은 새 DB나 endpoint 없이 기존 `videoUserRecords`에 `discovery-link:<기존 링크 ID>` 형태의 안정적인 작업 ID로 저장합니다.
- 링크 원본 URL과 제목은 기존 온라인 발견함(Azure DB) 기록을 계속 기준으로 사용합니다.
- `태그별 금고`는 별도 저장 장소로 오해되지 않도록 `채널 태그별 보기`로 바꾸고, 전체 수집 영상 목록으로 돌아가는 버튼을 연결했습니다.
- 오늘의 레이더에서 제작 후보가 생기면 제작 후보함, 대본 작업실, 업로드 캘린더로 바로 이어갈 수 있게 합니다.
- 운영에서 발견 링크 후보 → 대본 작업실 → 업로드 캘린더와 채널 태그별 보기 → 전체 수집 영상 목록 이동을 확인했습니다.
- 대본 작업실의 입력이 즉시 초기화되는 문제를 운영에서 발견해, 같은 Azure DB 기록 내용이 새 객체로 전달될 때 입력 중 초안을 다시 덮어쓰지 않도록 보완했습니다.
- 기존 제작 기록의 빈 통합 메모를 검수 문구로 저장하고 새로고침 후 같은 값을 재조회한 뒤, 원래 빈 값으로 복원·저장·재조회해 검수 흔적이 남지 않은 것을 확인했습니다.

## 2026-07-28 일일 흐름과 업로드 일정 후속 검수

- Jinxy 채널 1개를 오늘 볼 채널로 선택하고 Azure DB의 수집 영상 정보 102개를 불러왔습니다.
- 레이더 상위 후보 6개 → 제작 후보함 → 대본 작업실로 같은 제작 후보가 이어지는 것을 확인했습니다.
- 소재 보관함 해제는 원본 영상이나 수집 영상 정보를 삭제하지 않으며, 제작 후보 상태 변경은 Azure DB 판단 기록만 바꾼다는 안내를 확인했습니다.
- 기존 제작 후보의 업로드 예정일을 `2026-08-04`로 저장하고 캘린더 표시와 새로고침 후 재조회를 확인했습니다.
- 업로드 예정일을 원래 빈 값으로 복원·저장·재조회해 전체 일정 0건, 날짜 미정 2건으로 돌아온 것을 확인했습니다.
- 이 검수에서는 YouTube API 신규 수집을 실행하지 않았습니다.

## 2026-07-28 탐색 도구·최근 수집 후속·모바일 검수

- Jinxy 수집 영상 정보 102개에서 또터또 탐색의 기준 충족 후보가 0개임을 확인했습니다.
- 또터또 빈 결과의 `수집 영상 목록 화면 열기`가 수집 영상 102개 목록으로 정상 복귀했습니다.
- 키워드 탐색에서 추천어 `moments`를 누르면 수집 영상 검색 결과 50개, 일치 채널 1개가 표시됐습니다.
- 키워드 결과의 `제작 후보로`·`소재 보관`은 Azure DB 사용자 기록 변경, `댓글 Top 10(API)`는 YouTube API 호출임을 버튼 안내에서 구분했습니다.
- 최근 수집 상태의 Jinxy `이 채널 관리`가 Jinxy를 선택한 채널 운영실 1단계로 정상 연결됐습니다.
- 또터또 탐색, 키워드 탐색 결과, 최근 수집 상태, 채널 운영실을 390×844에서 확인했고 페이지 전체 가로 넘침이 없었습니다.
- 검수 중 Azure DB 저장·삭제와 YouTube API 신규 수집 버튼은 실행하지 않았습니다.

## 2026-07-29 운영 후속 1~5번 검수

- 최근 수집 상태의 검색, 최근 상태와 과거 목록 확인 상태 조합 필터, 정렬, 과거 실행 상세 펼치기를 운영에서 확인했습니다.
- 채널 운영실의 관리·등록·수집 영상 확인 단계를 실제 전환해 Azure DB 조회와 YouTube API 수집 안내가 분리돼 있음을 확인했습니다.
- 수집 영상 102개의 제목 검색, 최신순 정렬, 제작 후보 버튼 안내와 390×844 모바일 표시를 확인했습니다.
- 제작 후보 → 대본 작업실 → 업로드 캘린더 → 같은 후보 대본 이어쓰기 왕복을 확인했습니다.
- 설정 ↔ 업무 도구함 왕복과 도구 검색을 운영에서 확인했습니다.
- 채널을 선택하지 않았을 때 전체 운영중 채널을 수집할 수 있던 위험을 제거했습니다. 이제 채널을 하나 이상 명시적으로 선택해야 YouTube 새 영상 수집 버튼이 활성화됩니다.
- 실제 YouTube API 신규 수집·댓글 조회와 Azure DB 저장·삭제는 실행하지 않았습니다.

## 2026-07-29 수집 영상 선택·모바일·레이더 후속 검수

- 수집 영상 103개에서 카드 보기와 리스트 보기를 전환하고 영상 2개를 실제 선택했습니다.
- 선택 버튼은 정상적으로 바뀌었지만 AI 요청문 작업 막대가 나타나지 않는 오류를 발견했습니다. 선택 영상 ID 문자열을 영상 객체로 잘못 판정해 선택 수를 0개로 계산하던 원인을 수정했습니다.
- 390×844에서 카드 보기는 긴 제목과 제작 버튼이 화면 안에 표시됐고, 리스트 보기는 페이지 전체가 아니라 표 영역 안에서만 가로 스크롤됩니다.
- 최근 수집 상태의 실패 0건 빈 결과에는 검색·필터 초기화가 표시됐습니다. 과거 부분 성공 2건에는 현재 영향과 과거 영상 수집 다음 행동이 표시됐고, 해당 채널이 선택된 채널 운영실로 연결됐습니다.
- 오늘의 레이더의 기존 제작 후보 1건이 제작 후보함의 같은 영상으로 이어지는 것을 확인했습니다.
- 개선 기록의 미완료 2건은 `대본 수정 이력·이전 버전 비교`와 `AI 대본 보조`이며 모두 개인용 MVP 이후 항목입니다.
- 검수 중 Azure DB 저장·삭제, YouTube 신규 수집·댓글 조회는 실행하지 않았습니다.

## 2026-07-29 후속 1~5번 편의·효율 보완

- 일부 Azure DB 기능만 연결되지 않았을 때 전체 온라인 저장소가 실패한 것처럼 보이던 공통 경고를 `일부 온라인 저장 기능 확인 필요`로 좁혔습니다.
- 경고에 적힌 기능만 확인하면 되며, 채널 목록이나 수집 영상처럼 성공한 다른 Azure DB 조회 데이터는 정상이라는 설명을 추가했습니다.
- 영상 2개의 AI 요청문 복사를 실제로 실행해 제목, 조회수, 참여율, 포맷과 요청 사항이 포함된 505자 클립보드 내용을 확인했습니다.
- 검색·정렬·카드/리스트 전환·메뉴 왕복에서는 영상 선택을 유지하고, 새 채널 범위의 수집 영상을 불러올 때 초기화하는 기준을 표시했습니다.
- 필터로 선택 영상이 숨겨져도 사용자가 즉시 정리할 수 있도록 `선택 해제` 버튼을 추가했습니다. 선택 해제는 화면 선택만 바꾸고 Azure DB나 YouTube API에 영향을 주지 않습니다.
- 390×844 리스트 보기는 페이지 전체 가로 넘침 없이 표 내부에서 좌우 스크롤하며, 모바일에서는 카드 보기가 더 편하다는 안내를 추가했습니다.
- 저장·삭제와 YouTube 신규 수집 없이 확인하는 `매일 3분 확인 순서`를 정기 회귀 검수표에 확정했습니다.
